# VAL — Core Architecture Principle

1. One encrypted database per user.
2. One universal schema adaptable via tagging.
3. Modules are logic overlays, not separate systems.
4. Deterministic layers first.
5. LLM is narrative, never authoritative.
6. No silent data mutation.
7. Feature growth must not fork the core.

Universal core. Adaptive behavior.
